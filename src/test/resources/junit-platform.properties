# Enable parallel execution
junit.jupiter.execution.parallel.enabled = true

# Default execution mode for classes and methods (concurrent = parallel)
junit.jupiter.execution.parallel.mode.default = same_thread
junit.jupiter.execution.parallel.mode.classes.default = concurrent

# Configure fixed parallelism with 4 threads
# junit.jupiter.execution.parallel.config.strategy = fixed
# junit.jupiter.execution.parallel.config.fixed.parallelism = 4

# Set maximum pool size (prevents excessive thread creation when tests block)
# junit.jupiter.execution.parallel.config.fixed.max-pool-size = 8
#
## Add this to your src/test/resources directory
